import * as openai from "openai";
import { InstrumentationBase, InstrumentationConfig, InstrumentationModuleDefinition } from "@opentelemetry/instrumentation";
/**
 * function to check if instrumentation is enabled / disabled
 */
export declare function isPatched(): boolean;
export declare class OpenAIInstrumentation extends InstrumentationBase<typeof openai> {
    constructor(config?: InstrumentationConfig);
    protected init(): InstrumentationModuleDefinition<typeof openai>;
    /**
     * Manually instruments the OpenAI module. This is needed when the module is not loaded via require (commonjs)
     * @param {openai} module
     */
    manuallyInstrument(module: typeof openai): void;
    /**
     * Patches the OpenAI module
     */
    private patch;
    /**
     * Un-patches the OpenAI module's chat completions API
     */
    private unpatch;
}
//# sourceMappingURL=instrumentation.d.ts.map